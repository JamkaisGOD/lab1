[project]
name = "ku-turshilt"
version = "0.1.0"
description = "KU TURSHILT project environment"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "jupyterlab",
  "ipykernel",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
